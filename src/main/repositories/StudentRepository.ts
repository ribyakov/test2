// import { Student } from "../entities/student.entity";
// import { AppDataSource } from "../typeorm.config";
// import { Dialog, dialog } from "electron";

export default class StudentRepository {
  // public static async list() : Promise<Student[]> {
  //     const result = await AppDataSource.manager.find();
  //     return result;
  // }
  //
  // public static async upsert(data: {student: Partial<Student>}) {
  //     const studentToUpdate = AppDataSource.manager.create(Student, data.student);
  //     await AppDataSource.manager.save(studentToUpdate);
  // }
  //
  // public static async saveToFile() {
  //     let result = dialog.showOpenDialog({ properties: ['openFile', 'multiSelections'] });
  //     return result;
  // }
}
